export enum HttpMethods {
    POST = 'POST',
    GET = 'GET'
}

export enum HttpStatus {
    SUCCESS = 200,
    UNAUTHORIZED = 401,
    CREATED = 201,
    DELETED = 204,
    INTERNAL_SERVER_ERROR = 500,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
}
